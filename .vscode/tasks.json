{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Auto Setup Environment",
            "type": "shell",
            "windows": {
                "command": "if (-not (Test-Path '.venv')) { python -m venv .venv; .\\.venv\\Scripts\\python.exe -m pip install -r requirements.txt }"
            },
            "osx": {
                "command": "if [ ! -d '.venv' ]; then python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt; fi"
            },
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}